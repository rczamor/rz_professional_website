declare namespace React {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars -- T must match React's built-in HTMLAttributes<T> generic for declaration merging
  interface HTMLAttributes<T> {
    toolname?: string;
    tooldescription?: string;
    toolparamtitle?: string;
    toolparamdescription?: string;
  }
}
